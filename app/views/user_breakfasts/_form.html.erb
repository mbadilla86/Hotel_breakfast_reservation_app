<%= form_with(model: user_breakfast) do |form| %>
  <% if user_breakfast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_breakfast.errors.count, "error") %> prohibited this user_breakfast from being saved:</h2>

      <ul>
        <% user_breakfast.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :breakfast_id %>
    <%= form.text_field :breakfast_id %>
  </div>

  <div class="field">
    <%= form.label :place %>
    <%= form.select :place, @places %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.select :time, @times %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
